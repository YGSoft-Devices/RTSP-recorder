#pragma once

// Template pinout OV5640 (à compléter selon le modèle exact)
//
// Important:
// - L’OV5640 existe sur plusieurs cartes (ESP32-WROVER / ESP-EYE / S3-EYE / clones).
// - Le mapping GPIO varie fortement.
//
// Compléter les macros ci-dessous puis compiler avec un flag (ex: -D CAMERA_BOARD_CUSTOM_OV5640=1)

#error "OV5640 pinout non défini: compléter esp32/firmware/include/boards/ov5640_template.h puis activer CAMERA_BOARD_CUSTOM_OV5640"

